Place your built Apple Silicon installer here (only this file is linked on the site):

  NyeClock-1.0.0-arm64.dmg

The version number must match NYE_DESKTOP_VERSION in ../index.html and desktop/package.json.

Intel (x64) Macs: no desktop build is provided — use the web app from the landing page.

After adding the file, deploy Cloudflare Pages so /website/downloads/ serves it.
